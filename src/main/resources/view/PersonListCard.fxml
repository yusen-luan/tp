<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.FlowPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<HBox id="cardPane" fx:id="cardPane" xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1">
  <VBox HBox.hgrow="ALWAYS" spacing="10">
    <padding>
      <Insets top="14" right="16" bottom="14" left="16" />
    </padding>

    <!-- HEADER: Badge, Name, and Student ID on single row -->
    <HBox alignment="CENTER_LEFT" spacing="10">
      <!-- Index badge -->
      <Label fx:id="id" styleClass="id-badge-compact" style="-fx-min-width: 60; -fx-pref-width: 60; -fx-alignment: center;">
        <padding>
          <Insets top="4" right="6" bottom="4" left="6" />
        </padding>
      </Label>

      <!-- Name and Student ID stacked -->
      <VBox spacing="1" HBox.hgrow="ALWAYS">
        <Label fx:id="name" text="\$first" styleClass="name-label-compact" wrapText="true" maxWidth="Infinity" />
        <Label fx:id="studentId" text="\$studentId" styleClass="student-id-label" />
      </VBox>
    </HBox>

    <!-- ACADEMIC INFO - Compact Row -->
    <HBox spacing="12" alignment="CENTER_LEFT" styleClass="academic-section-compact">
      <padding>
        <Insets top="8" right="10" bottom="8" left="10" />
      </padding>

      <!-- Module -->
      <HBox spacing="6" alignment="CENTER_LEFT">
        <Label text="📚" styleClass="icon-compact" />
        <Label fx:id="moduleCode" text="\$modules" styleClass="field-value-compact" />
      </HBox>
    </HBox>

    <!-- ATTENDANCE ROW - 13 Weeks with styled box -->
    <HBox spacing="12" styleClass="attendance-section-box" alignment="CENTER_LEFT">
      <padding>
        <Insets top="8" right="10" bottom="8" left="10" />
      </padding>

      <Label text="📊" styleClass="icon-compact" style="-fx-font-size: 13pt;" />

      <VBox spacing="2" HBox.hgrow="ALWAYS">
        <!-- Week numbers row (top) -->
        <HBox fx:id="weekNumbersRow" spacing="10" alignment="TOP_LEFT" />

        <!-- Attendance status row with colored rectangles (bottom) -->
        <HBox fx:id="attendanceRow" spacing="10" alignment="TOP_LEFT" />
      </VBox>
    </HBox>

    <!-- GRADES - Compact inline -->
    <HBox spacing="6" alignment="CENTER_LEFT" styleClass="grades-section-compact">
      <padding>
        <Insets top="8" right="10" bottom="8" left="10" />
      </padding>

      <Label text="🎓" styleClass="icon-compact" style="-fx-font-size: 13pt;" />
      <FlowPane fx:id="grades" hgap="6" vgap="6" HBox.hgrow="ALWAYS" />
    </HBox>

    <!-- CONSULTATIONS - Compact inline -->
    <HBox spacing="6" alignment="CENTER_LEFT" styleClass="consultations-section-compact">
      <padding>
        <Insets top="8" right="10" bottom="8" left="10" />
      </padding>

      <Label text="📅" styleClass="icon-compact" style="-fx-font-size: 13pt;" />
      <Label fx:id="consultations" text="\$consultations" styleClass="field-value-compact" wrapText="true" />
    </HBox>

    <!-- REMARK - Compact inline -->
    <HBox spacing="6" alignment="CENTER_LEFT" styleClass="remark-section-compact">
      <padding>
        <Insets top="8" right="10" bottom="8" left="10" />
      </padding>

      <Label text="📝" styleClass="icon-compact" style="-fx-font-size: 13pt;" />
      <Label fx:id="remark" text="\$remark" styleClass="field-value-compact" wrapText="true" />
    </HBox>

    <!-- TAGS - Compact inline -->
    <FlowPane fx:id="tags" hgap="6" vgap="6" />

  </VBox>
</HBox>
